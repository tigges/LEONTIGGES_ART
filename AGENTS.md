# AGENTS.md

## Cursor Cloud specific instructions

This repository ("LEONTIGGES_ART") is currently an empty/blank project with only a `README.md`. There is no application code, no package manager lockfile, no build system, and no services to run.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or similar.
- **No services to start** — no backend, frontend, database, or other runtime components.
- **No lint/test/build commands** — no test framework or CI configuration is present.

When application code is added to this repository, update this file with:
1. How to install dependencies (update script).
2. How to run, build, lint, and test the application.
3. Any non-obvious development environment caveats.
